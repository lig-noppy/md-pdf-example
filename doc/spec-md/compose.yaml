name: spec-md

services:
  md-pdf:
    image: ghcr.io/lig-noppy/md-pdf:latest
    env_file:
      - path: .env
        required: false
    environment:
      LANG: ${LANG:-ja_JP.UTF-8}
      LANGUAGE: ${LANGUAGE:-ja_JP:ja}
      LC_ALL: ${LC_ALL:-ja_JP.UTF-8}
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./work:/work
